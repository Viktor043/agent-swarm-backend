[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r dashboard/requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "cd dashboard && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
